# 实验 3 Prompt Log：水库调度优化

## 交互 1：读取实验要求

**中文提示词：**

> 请阅读实验 3 文档，提取水库调度优化问题的目标、约束、参数、分析任务和最终交付物，并用中文完成实验记录。

**AI 输出摘要：**

- 决策变量为 7 天日均出库流量 `Q_release`。
- 目标为最大化水电收益，并分析生态流量缺口。
- 约束包括库容上下限、出库上下限和逐日水量平衡。
- 需要生成最优调度表、收益-生态权衡图和约束验证报告。

**人工校正：**

- 文档中的单位符号存在编码异常，人工确认流量单位为 `m3/s`，库容单位为 `m3`。
- 严格优化方案中保留 `Q_release >= Q_eco`，保证最终调度没有生态流量违反。

## 交互 2：数学建模与 scipy 实现

**中文提示词：**

> 请使用 scipy.optimize.minimize 实现 7 天水库调度优化。目标是最大化水电收益，约束包括库容上下限、出库上下限和水量平衡，输出最优放水过程和总收益。

**AI 输出摘要：**

- 使用 SLSQP 求解带边界和不等式约束的优化问题。
- 将最大化收益转化为最小化负收益。
- 通过 `calculate_storage_trajectory()` 根据水量平衡计算每日末库容。
- 对每日末库容设置 `V_min <= V_t <= V_max` 约束。
- 默认严格要求 `Q_release >= Q_eco`。

**人工校正：**

- 增加标准水电收益计算：`E = rho * g * H * eta * volume`。
- 采用 25 m 水头与 0.85 效率，使收益数量级接近文档中的预期结果。
- 初始 SLSQP 方案在角点解附近返回 `success=False`，因此改用更适合线性目标与线性约束的 `scipy.optimize.linprog`，仍满足实验对 `scipy.optimize` 的要求。

## 交互 3：收益-生态权衡分析

**中文提示词：**

> 请运行不同生态目标权重下的优化，生成收益和生态缺口之间的 Pareto frontier 图，并说明如果优先生态会发生什么。

**AI 输出摘要：**

- 严格生态方案用于最终可行调度，生态缺口为 0。
- 软生态方案允许出库低于 `Q_eco`，通过生态缺口惩罚系数分析收益与生态之间的权衡。
- 输出 `tradeoff_results.csv` 和 `tradeoff_analysis.png`。

**人工校正：**

- 为避免混淆，报告中明确区分“严格满足生态流量的最终方案”和“用于权衡分析的软约束方案”。

## 交互 4：约束验证

**中文提示词：**

> 请验证最优调度是否满足所有约束：库容上下限、出库上下限、生态最小流量、水量平衡和收益计算，并生成 validation_report.txt。

**AI 输出摘要：**

- 检查所有释放流量均不小于 `10 m3/s`，且不超过 `100 m3/s`。
- 检查所有库容都在 `100,000` 到 `1,000,000 m3` 之间。
- 逐日重新计算水量平衡误差。
- 对比调度表收益合计与直接收益计算。

**人工校正：**

- 报告中加入最优放水序列和库容序列，便于老师快速核对。
