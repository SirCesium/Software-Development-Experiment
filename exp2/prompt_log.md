# 实验 2 Prompt Log：SCS-CN 降雨径流计算

## 交互 1：读取实验要求

**中文提示词：**

> 请阅读实验 2 文档，提取需要完成的代码、测试、敏感性分析、物理验证和最终交付物，并在实现过程中使用中文记录实验过程。

**AI 输出摘要：**

- 主程序需要实现 SCS-CN 径流公式。
- 测试需要覆盖 `P = 0`、`P < Ia`、`P = Ia`、正常案例、`CN = 100`、`Q <= P`。
- 敏感性分析需要固定 `P = 50 mm`，比较 `CN = [60, 70, 80, 90, 95, 100]`。
- 需要生成 CN vs Q 图，以及不同 CN 条件下 Rainfall vs Runoff 对比图。
- 需要记录 AI 错误、人工修正和物理合理性验证。

**人工校正：**

- 文档中的公式符号由于编码显示异常，人工确认公式为 `Q = (P - Ia)^2 / (P - Ia + S)`。
- 明确 `P <= Ia` 时径流应为 0，避免在 `P = Ia` 边界产生浮点误差。

## 交互 2：实现 SCS-CN 公式

**中文提示词：**

> 请写一个 `calculate_runoff(P, CN)` 函数，按 SCS-CN 公式计算径流深，并处理 `CN=0`、`CN=100`、`P<Ia`、`Q<=P` 等边界条件。函数需要有类型提示和文档字符串。

**AI 输出摘要：**

- `calculate_retention()` 计算 `S = 25400 / CN - 254`。
- `calculate_runoff()` 计算 `Ia = 0.2S` 与径流深 `Q`。
- `CN = 0` 时返回 0，表示全部入渗。
- `CN = 100` 时返回 `P`，表示不透水地表。
- 使用 `np.clip()` 保证 `0 <= Q <= P`。
- 支持标量和 NumPy 数组输入。

**人工校正：**

- 增加 `ValueError` 检查，防止负降雨或超出 `[0, 100]` 范围的 CN。

## 交互 3：编写边界测试

**中文提示词：**

> 请根据实验文档 Part 2 写完整测试：P=0、P<Ia、P=Ia、P=50/CN=80、CN=100、CN=0，并验证所有情况下 Q 不超过 P。

**AI 输出摘要：**

- 使用 Python `unittest` 编写 `test_scscn.py`。
- 对 `P = 50 mm, CN = 80` 验证结果约为 `13.8 mm`。
- 对多个降雨值和 CN 值循环检查 `Q <= P`。
- 检查 CN 增大时径流不减少。

**人工校正：**

- 增加向量化输入测试，保证敏感性分析可直接使用 NumPy 数组。

## 交互 4：生成敏感性分析图

**中文提示词：**

> 请固定 P=50mm，计算 CN=[60,70,80,90,95,100] 的 Q，并生成两个图：CN vs Q，以及不同 CN 下 Rainfall vs Runoff 对比图。

**AI 输出摘要：**

- 生成 `sensitivity_analysis.py`。
- 输出 `sensitivity_results.csv`。
- 生成 `runoff_comparison.png`，左图为 CN vs Q，右图为不同 CN 的降雨-径流曲线。

**人工校正：**

- 图中加入 `Q = P` 虚线，帮助验证径流不超过降雨。
- 使用中文标题与坐标轴，便于课堂报告展示。

## 交互 5：物理验证与文档整理

**中文提示词：**

> 请整理实验 2 的验证记录，说明边界条件是否通过、Q 是否总小于等于 P、CN 增大为何导致径流增大，并列出最终交付文件。

**AI 输出摘要：**

- 补充 `validation_report.md`。
- 补充 `README.md`，说明实验任务对应的文件位置。
- 明确五个 Deliverables：`scscn_runoff.py`、`test_scscn.py`、`sensitivity_analysis.py`、`runoff_comparison.png`、`prompt_log.md`。

**人工校正：**

- 将“真实物理规律”和“程序测试结果”分开说明，避免只给出代码结果而没有水文学解释。
